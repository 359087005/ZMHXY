# 窗口后台实时预览 & 自动化工具

在不激活窗口的前提下，实时预览目标程序画面，并支持后台自动化操作（点击等）。

## 环境要求

- Windows 10 / 11
- Python 3.10+
- 依赖：`opencv-python`

## 运行

```bash
pip install -r requirements.txt
python window_capture.py
```

> 如果目标是以管理员运行的游戏，需要以管理员身份运行 Python，否则 Windows UIPI 会阻止所有输入注入。

## 文件说明

| 文件 | 作用 |
|---|---|
| `window_capture.py` | 主程序 GUI，窗口预览、Debug 放大镜、脚本执行 |
| `script_action.py` | 自动化动作库，封装后台点击、拖动、按键、模板识别等能力 |
| `game_logic.py` | 当前“执行脚本”按钮的逻辑入口，按勾选功能块执行游戏内流程，热重载后直接执行 |

## 功能一览

### 权限处理
- 标题栏会显示当前是“普通权限”还是“管理员权限”
- 程序启动时会自动申请管理员权限，不再需要手动点击「管理员重启」
- 新增「管理员重启」按钮：通过 UAC 直接重启当前程序为管理员权限
- 源码运行时，自动管理员重启会优先使用 `pythonw.exe`，尽量避免额外弹出控制台窗口
- 当日志里出现 `err=5 / 拒绝访问` 时，优先处理权限问题，而不是继续切换窗口策略

### 智能锚定
- 下拉框仍显示 Alt+Tab 可见的标题窗口，便于选择
- 点击「锚定窗口」后，会自动扫描**同进程的所有顶层窗口**
- 如果当前选中的句柄只是标题壳窗口，会自动切换到同进程里更大的真实渲染/输入窗口
- 候选窗口列表、面积、类名、矩形信息都会写入日志面板

### 窗口实时预览
- 使用 **DWM Thumbnail API** (`DwmRegisterThumbnail`) 直接从 GPU 合成器获取画面
- 与 Windows 任务栏悬停预览使用相同机制，可观察 DWM 当前持有的合成画面
- 默认实时预览属于 **DWM/GPU 合成预览**
- 可切换显示完整窗口或仅客户区
- 注意：这里拿到的是 **DWM 当前缓存/合成的帧**，不是“强制游戏继续渲染”的能力
- 如果游戏在失焦、边缘停靠、完全遮挡后停止 `Present` 或降低帧率，预览也会跟着冻结或低频更新
- 主预览现在支持直接设置**主预览间隔（秒）**，填 `0` 表示继续实时显示 DWM/GPU 预览
- 当主预览间隔大于 `0` 时，程序会按设定节奏短暂挂载 `DWM thumbnail` 取一帧，再注销并显示静态快照，避免主预览持续高频刷新
- 新增「开启采样预览」按钮：主动调用 `PrintWindow` / 位图抓取刷新预览，不再只依赖 DWM 缩略图
- 采样预览属于 **PrintWindow / GDI 采样链路**，不是 GPU 合成预览
- 当“脚本生效但 DWM 预览静止”时，优先尝试采样预览模式
- 采样预览已做性能优化：更快的像素转换、更高刷新频率、失败自动快速重试
- 采样预览现在支持直接设置**采样间隔（秒）**，例如填 `1` 或 `2` 表示每 1 秒 / 2 秒采样一次
- 采样预览开启后修改间隔也会即时生效，不需要先停掉再重开
- 开启采样预览时，主界面的 DWM/GPU 预览会临时隐藏，避免两种预览叠在一起导致“间隔看起来没变化”
- 采样预览的 `Tk PhotoImage` 输入已从 `PPM data` 改为 `PNG data`，规避“抓帧成功但 Tk 无法解析图像”的问题
- 预览区支持**点击反推坐标**：点击画面任意一点，会自动计算目标窗口中的实际坐标
- 如果当前显示的是客户区，点击后会直接回填到 Debug 的 `X / Y`
- 如果当前显示的是完整窗口，点击标题栏/边框时会提示“该点不在客户区内”
- 点击预览后会显示十字准星，方便连续校准位置；该标记约 5 秒后会自动消失
- 命中客户区时，会自动把 `TARGET_X / TARGET_Y` 复制到剪贴板

### 后台运行模式
- 新增「转后台保活」按钮：将目标窗口停靠到屏幕边缘，仅保留极小可见角，保持**非最小化**状态
- 新增「强制窗口化」按钮：尝试把目标进程的顶层窗口改成普通窗口样式，便于后续移动
- 新增「设置分辨率」按钮：可把当前锚定窗口的**客户区尺寸**调整到指定宽高，默认 `800x600`
- 新增「任务栏图标: 隐藏/显示」切换按钮：修改窗口 `EXSTYLE`，尝试切换任务栏按钮显示状态
- 新增「恢复原位」按钮：把窗口恢复到原始位置/状态
- 关闭本工具时也会自动尝试恢复目标窗口原位、窗口样式与任务栏图标状态，避免忘记手动点「恢复原位」
- 这一模式专门用于规避“游戏最小化后暂停渲染/暂停 UI 逻辑”的问题
- 对游戏而言，**不要再手动最小化**；应优先使用后台运行模式
- 当前实现会尝试停靠**同进程的所有顶层窗口**，并在日志中输出移动前后矩形
- 之所以不再完全移出屏幕，是因为部分游戏在“完全离屏/完全遮挡”后会停止渲染，导致实时预览冻结
- 如果停靠后仍然没有进入保活位置，通常说明该游戏是**独占全屏**或**自管理窗口**，标准 Win32 移动无效
- 如果 `转后台保活` 无效，可先点击 `强制窗口化` 再重试
- 任务栏图标切换通常会同时影响 Alt+Tab 可见性，具体效果取决于目标程序窗口类型

### Debug 放大镜
- 输入坐标 (X, Y) 和区域大小，放大查看指定位置的像素级细节
- 使用 DWM `rcSource` 裁切，GPU 渲染 6 倍放大
- 用于精确确认点击坐标
- 新增 `Debug 点击` 按钮：直接对当前 Debug 坐标执行一次普通后台点击
- 新增 `强力点击回退` 按钮：对当前 Debug 坐标执行更强的保底点击（先后台，再短暂前台硬件回退）
- 脚本和 Debug 触发的虚拟点击，现在会在预览窗口里短暂闪出坐标标记，便于对照后台点击是否打在预期位置

### 自动化脚本 (`script_action.py`)
- 提供以后台消息为主的自动化方法，适合在 `run()` 中顺序编排动作
- 内置诊断功能，检测管理员权限、窗口类名、子窗口结构等
- 默认会先确保窗口处于“非最小化”的后台运行状态，再尝试点击
- `script_action.py` 已整理为 `WindowAutomation` 调用风格，适合顺序编排大量动作
- 主界面的「执行脚本」当前会热重载并执行 `game_logic.py`；`script_action.py` 作为底层动作库被复用
- 模板识别现在会缓存已加载模板，降低长时间轮询时的重复读盘、重复解码和内存抖动
- 主界面支持勾选功能块；只有勾选的功能块才会在 `game_logic.py` 中执行
- 主界面新增 `抓鬼任务` 功能块，以及附加选项 `超20抓鬼`
- 仅勾选支持直跑的任务时，会直接进入对应模块，不再先走任务界面模板识别；当前包括 `挖宝图`、`秘境降妖`、`抓鬼任务`
- `秘境降妖` 已接入基础流程，依赖 `templates/MJXY/` 下的 `jm_mjxy`、`jm_mjxy_tj`、`jm_mjxy_yyl`、`mjxy_jr`、`mjxy_jxtz`
- `抓鬼任务` 已接入基础流程，依赖 `templates/5抓鬼任务/` 下的 `jm_zg2`、`jm_zg`、`jm_zgrw`、`jm_jxzg`
- `tasks/shared_rects.py` 维护任务共用矩形范围，例如 `HUODONG_SEARCH_RECT`、`BAG_SEARCH_RANGE`、`CANCEL_BUTTON_RECT`
- 主界面新增「终止脚本」按钮：采用协作式终止，收到请求后会在当前步骤结束点安全退出
- 常用鼠标方法：`move_to()`、`click()`、`click_robust()`、`double_click()`、`right_click()`、`drag()`
- 新增 `force_clear_ui()`：默认在 `(407, 267)` 连续右键 `5` 次，每次间隔 `1` 秒，用于强制清除界面内容
- 常用键盘方法：`key_down()`、`key_up()`、`press_key()`、`hotkey()`、`send_text()`
- 常用识别方法：`find_image()`、`wait_image()`、`click_image()`、`click_image_robust()`
- 随机偏移规则：`click(350, 375, 5)` 表示最终点击坐标会在 `x/y` 的 `±5` 范围内随机偏移
- `drag()` 现在支持 `duration_sec`，用于控制从 A 到 B 的总拖动时长；默认约 `1.0` 秒
- 模板图默认放在 `templates/` 目录；可直接传文件名如 `start_button` / `start_button.png`

### 模板识别
- 已直接接入 `OpenCV` 模板匹配，适合“按钮样式固定，但位置不固定”的场景
- 默认在**整个客户区**内查找模板，不要求按钮在固定区域
- 现在支持 `search_rect=(left, top, right, bottom)`，可把搜索范围限制在指定客户区矩形内
- `find_image("start_button")`：返回模板中心点与匹配分数
- `find_image("start_button", search_rect=(402, 159, 717, 469))`：只在指定矩形框内找图
- `wait_image("start_button", timeout_sec=5)`：等待模板出现
- `click_image("start_button")`：识别后普通点击
- `click_image_robust("start_button")`：识别后强力点击回退
- 默认模板目录为 `templates/`，支持 `.png/.jpg/.jpeg/.bmp/.webp`
- 模板读取已兼容 Windows 中文路径目录，例如 `templates/BTRW/`

### 输入动作

| # | 动作 | 后台？ | 原理 | 适用场景 |
|---|---|---|---|---|
| 1 | `click()` / `double_click()` / `right_click()` | 完全后台 | 单一 `SendMessage` 鼠标消息链 | 普通点击、双击、右键 |
| 2 | `move_to()` | 完全后台 | 发送 `WM_MOUSEMOVE` | 悬停、预热鼠标位置 |
| 3 | `click_robust()` | 先后台后前台保底 | 先走后台点击，再临时前台化做一次硬件级点击 | 普通点击不吃的软件 |
| 4 | `find_image()` / `wait_image()` | 完全后台 | OpenCV 模板匹配 | 固定按钮、固定 UI、位置不固定 |
| 5 | `click_image()` / `click_image_robust()` | 识别后点击 | 模板匹配 + 点击链路 | 识别后自动点击 |
| 6 | `drag()` | 完全后台 | `SendMessage` 拖拽消息链 | 列表拖动、滑动条、拖拽操作 |
| 7 | `key_down()` / `key_up()` / `press_key()` | 完全后台 | 键盘消息链 | 回车、方向键、功能键等 |
| 8 | `hotkey()` / `send_text()` | 完全后台 | 组合键与 `WM_CHAR` 文本输入 | 快捷键、输入框文本填充 |
| 9 | `子窗口自动映射` | 完全后台 | 自动把主窗口客户区坐标映射到覆盖该点的子窗口 | UI 落在子窗口的场景 |

> 说明：鼠标点击类动作现在只保留一套 `SendMessage` 链路，避免重复发三套消息；如果你已经验证当前游戏这套能吃，脚本会更干净也更容易维护。

### 诊断功能
- 自动检测脚本/目标进程的管理员权限是否匹配
- 枚举窗口类名、客户区大小、子窗口结构
- 输出窗口是否最小化、是否已处于屏幕外后台运行状态
- `转后台运行` / `强制窗口化` 会输出关键 Win32 API 的返回值和 `GetLastError`
- 诊断输出显示在 GUI 日志区域
- 日志面板会自动裁剪旧日志，只保留最近一段内容，避免长时间运行后内存持续上涨

### 当前推荐使用方式
1. 先看标题栏权限状态；如果是普通权限，先点击「管理员重启」
2. 锚定目标窗口
3. 如果目标像独占全屏游戏，先点击「强制窗口化」
4. 如需固定窗口尺寸，可先在主界面填入宽高后点击「设置分辨率」，默认 `800x600`
5. 点击「转后台保活」，让窗口保持非最小化并停靠到屏幕边缘
6. 如果你不需要主预览太高频，可先把「主预览间隔」设为 `0.5 / 1 / 2` 秒
7. 如果 DWM 预览仍冻结，先设置采样间隔，再点击「开启采样预览」
8. 直接点击预览画面反推坐标；命中客户区后会显示准星并自动复制 `TARGET_X / TARGET_Y`
9. 勾选本次要执行的功能块，例如「挖宝图」或「抓鬼任务」
10. 点击「执行脚本」，当前会热重载并执行 `game_logic.py`
11. 如果本次只勾选支持直跑的任务，会直接执行对应逻辑，不依赖 `templates/RWJM/` 下的任务界面入口模板
12. `抓鬼任务` 如勾选 `超20抓鬼`，会先走地图直达钟馗，再进入抓鬼轮询逻辑
13. 单独跑 `挖宝图`、`秘境降妖`、`抓鬼任务` 时，仍需准备各自模板目录下的素材
14. 如需中途停止，点击「终止脚本」
15. 在 `game_logic.py` 中编排游戏流程，在 `script_action.py` 中补通用动作和识别能力

---

## 变更日志

### v38 — 抓鬼轮询调整 + 长跑内存优化
- `tasks/zhuagui.py` 的 `超20抓鬼` 导航流程中，点击小地图后改为等待 8 秒
- 其它导航点击仍保持原本的短等待，避免切图尚未完成就继续点钟馗
- 抓鬼轮询里，未找到 `jm_jxzg.png` 后会再补判一次战斗中的 `quxiao.png`；只有两边都连续未命中超过 6 次时才退出
- `window_capture.py` 的日志面板改为自动裁剪旧日志，避免长时间脚本输出导致 `Text` 内容无限累积
- `script_action.py` 的模板加载增加了按文件路径和修改时间生效的缓存，减少长时间识图时的重复读盘与解码
- `script_action.py` 新增 `force_clear_ui()`，默认会在 `(407, 267)` 连续右键 `5` 次、每次间隔 `1` 秒

### v37 — 抓鬼任务 + 公共矩形常量
- 主界面新增 `抓鬼任务` 功能块，以及附加选项 `超20抓鬼`
- 新增 `tasks/zhuagui.py`，支持活动界面进抓鬼，或在勾选 `超20抓鬼` 时先走地图直达钟馗再进入抓鬼流程
- 新增 `tasks/shared_rects.py`，把 `HUODONG_SEARCH_RECT`、`BAG_SEARCH_RANGE`、`CANCEL_BUTTON_RECT` 统一抽成任务共用常量

### v36 — 自动提权优先走 pythonw
- `window_capture.py` 的自动管理员重启逻辑现在会在源码运行时优先使用同目录下的 `pythonw.exe`
- 这样从普通权限自动拉起管理员实例时，通常不会再额外弹出黑色控制台窗口
- 打包运行时仍沿用原始可执行文件自身重启，避免影响已冻结分发形式

### v36 — 活动界面调度重写
- 重写 `tasks/renwujiemian.py`，改用 `Alt+C` 快捷键打开活动界面，替代原来的全图匹配任务界面图标
- 打开后先向下拖拽 4 次滚到列表顶部，再在列表区域匹配任务图标（支持普通 + 推荐两张模板）
- 找到任务图标后，在图标区域内二次匹配"参加"按钮并点击
- 当前页未找到支持向上翻页查找，最多翻 2 次
- 支持打宝图、师门、秘境降妖、押镖、三界奇缘、科举乡试六个任务的调度
- 模板统一放在 `templates/RWJM/` 下

### v35 — 挖宝图连续挖掘修复
- 修复挖宝图只挖一张就退出的问题：点击"使用"后不再返回主循环重新找背包，而是留在等待循环中继续等下一次"使用"按钮
- 利用游戏自动连续挖图机制，脚本只需双击第一张宝图启动，后续由游戏自动跑图，脚本负责检测并点击"使用"
- 全部挖完后（连续 20s 无"使用"或"取消"）自动结束

### v34 — 预览闪显虚拟点击
- 主预览叠层新增“虚拟点击闪显”标记：脚本和 Debug 触发的后台单击、双击、右击都会在预览里短暂显示坐标
- 手动点击预览得到的十字准星现在也会在约 5 秒后自动消失，避免长期遮挡画面
- 该标记不会覆盖原有的手动预览十字准星，主要用于排查脚本点击是否落在预期位置
- 双击会按两次实际点击分别闪显，便于对照点击节奏

### v32 — 秘境降妖基础流程
- 新增 `tasks/mijingxiangyao.py`，用于承接任务界面点击“前往”后的 `秘境降妖` 流程
- 当前流程会按顺序执行 `Alt+C`、识别 `jm_mjxy / jm_mjxy_tj`、点击 `jm_mjxy_yyl`、可选处理 `mjxy_jr`、再识别 `mjxy_jxtz`
- `tasks/renwujiemian.py` 的 `秘境降妖` 入口已切换到新模块，README 同步补充所需模板名

### v31 — 模板读取兼容中文路径
- `script_action.py` 的模板加载改为优先使用 `np.fromfile(...) + cv2.imdecode(...)`
- 修复 Windows 下模板位于中文目录名时，`cv2.imread()` 可能返回空对象导致“模板图读取失败”的问题
- 例如 `templates/BTRW/bb_baotu.png` 这类路径现在可以直接读取

### v30 — 挖宝图单独执行直跑
- 仅勾选 `挖宝图` 时，`game_logic.py` 会直接调用 `tasks/wabaotu.py`，不再经过 `tasks/renwujiemian.py`
- 这样单独运行挖宝图时，不会再因为缺少 `templates/RWJM/renwu_icon.png` 而提前报错
- README 同步补充了单任务直跑行为，以及 `templates/BTRW/` 仍需保留的模板要求

### v29 — 拖动支持总时长参数
- `script_action.py` 的 `drag()` 新增 `duration_sec` 参数，用于控制从起点到终点的总拖动时长
- 默认拖动时长改为约 `1.0` 秒，避免像背包翻页这类操作过快
- 仍保留 `step_delay_sec` 作为手动兜底参数；当传入 `duration_sec` 时会优先按总时长自动换算步进延迟

### v28 — 终止脚本按钮 + 功能块勾选
- 主界面的脚本区新增「终止脚本」按钮，可对当前运行中的 `game_logic.py` 发送终止请求
- 新增功能块勾选项：`挖宝图`、`打宝图`、`运镖`、`师门任务`、`三界奇缘`、`科举乡试`、`秘境降妖`
- 只有勾选的功能块才会执行；当前先落地了 `挖宝图`，其它勾选项暂时会记录“未实现，跳过”
- `game_logic.py` 现在支持按勾选功能块执行，并对终止请求做协作式退出

### v27 — 游戏逻辑入口 + 挖宝图基础流程
- 新增 `game_logic.py`，主界面的「执行脚本」现在会直接热重载并执行这个文件
- `script_action.py` 的模板识别新增 `search_rect` 参数，可把模板匹配限制在指定客户区矩形内
- 先落了一版“挖宝图”基础逻辑：打开背包、限定区域找 `bb_baotu.png`、翻页拖动、双击宝图、轮询 `jm_shiyong` / `quxiao.png`
- README 同步补充新的脚本入口和限定区域匹配用法

### v26 — 设置分辨率按钮
- 主界面新增宽高输入框与「设置分辨率」按钮，默认值为 `800x600`
- 点击后会尝试把当前锚定窗口的**客户区尺寸**调整到指定大小，而不是只改外框尺寸
- 调整前会先保存原始窗口位置/尺寸，便于后续点击「恢复原位」或关闭程序时自动恢复
- 调整后会自动刷新预览链路，避免预览仍沿用旧尺寸
### v25 — 主预览限帧修正 + 关闭时自动恢复
- 修正主预览限帧逻辑：不再单纯依赖 `fVisible=False`，而是到点才短暂注册 `DWM thumbnail`、取帧后立刻注销
- 这样在不同机器上更容易真正看到“主预览停住，按间隔更新一次”的效果
- 关闭程序时会自动恢复目标窗口原位、窗口样式与任务栏图标状态，降低忘记手动恢复的风险

### v24 — 主预览间隔可配置
- 主界面新增「主预览间隔」输入，单位为秒
- 填 `0` 表示继续实时 DWM/GPU 预览；大于 `0` 时按设定间隔更新主预览
- 这一模式保留了 DWM/GPU 预览来源，但降低了界面显示刷新频率
- 与「采样间隔」分离：前者控制主预览节奏，后者只控制采样预览节奏
- README 同步补充主预览间隔说明

### v23 — OpenCV 模板识别接入 + 强力点击按钮拆分
- 项目直接接入 `OpenCV` 模板匹配，可在 `script_action.py` 中识别固定按钮后自动点击
- `WindowAutomation` 新增 `find_image()`、`wait_image()`、`click_image()`、`click_image_robust()`
- 模板识别默认搜索整个客户区，适合“UI 固定但位置不固定”的场景
- `Debug 点击` 恢复为普通后台点击；新增独立的 `强力点击回退` 按钮
- `requirements.txt` 更新为 `opencv-python`
- README 同步补充模板目录约定与调用方式

### v22 — 自动管理员启动 + Debug 点击保底增强
- 程序启动时会自动申请管理员权限，避免每次再手动点「管理员重启」
- `Debug 点击` 改为更强保底链路：先走后台点击，再短暂前台化做一次硬件级点击
- 强力 Debug 点击结束后会恢复原窗口位置，尽量减少对当前工作流的影响
- README 同步补充自动提权与 Debug 点击保底说明

### v21 — 采样模式可见性修复 + Debug 点击
- 修复“采样间隔改了但看起来没生效”的问题：开启采样预览时会临时隐藏 DWM/GPU 预览，避免视觉叠层
- README 明确区分了默认 DWM/GPU 预览与采样预览的链路差异
- Debug 区新增 `Debug 点击` 按钮，可直接对当前 Debug 坐标执行一次模拟点击
- 顶部工具栏按钮改为更紧凑的样式，宽度更贴近按钮文字
- README 同步补充本次行为变化

### v20 — 采样预览间隔可配置
- 主界面新增「采样间隔」输入，单位为秒
- 支持直接设置为 `1`、`2` 等秒数，控制采样预览频率
- 采样预览开启后修改间隔会即时应用，不需要重开预览
- 当采样间隔被调得明显更慢时，抓帧失败后的重试也会跟着放慢，避免仍然高频打点
- README 同步补充采样间隔设置说明

### v19 — 单策略精简 + 动作库扩充
- 鼠标点击类动作只保留单一 `SendMessage` 链路，去掉三套重复点击策略
- `WindowAutomation` 新增 `move_to()`、`right_click()`、`key_down()`、`key_up()`、`press_key()`、`hotkey()`、`send_text()`
- `click()` / `double_click()` 接口简化，不再暴露策略选择参数
- README 同步更新为新的单策略与动作库说明

### v18 — 采样预览改 PNG + 脚本接口重构
- 采样预览的 Tk 图像输入从 `PPM data` 改为 `PNG data`，解决“抓帧成功但显示失败”的问题
- `script_action.py` 清理旧的试验性逻辑，改成更适合顺序脚本的 `WindowAutomation` 结构
- 新增 `prepare()`、`wait()`、`click()`、`double_click()`、`drag()` 方法
- 点击与拖动支持随机偏移参数，便于做轻微扰动点击
- `run()` 保留最小示例，后续可直接按顺序追加动作

### v17 — 预览准星 + 自动复制坐标
- 预览区新增十字准星覆盖层，点击后会把选中点直接标出来
- 准星会跟随预览布局变化自动重绘，窗口缩放后不容易漂
- 点击客户区后，会自动复制 `TARGET_X / TARGET_Y` 到剪贴板，方便直接粘到脚本
- 非客户区点击仍会显示窗口坐标，但不会误复制成脚本坐标
- README 同步补充准星和剪贴板行为说明

### v16 — 预览点选取坐标 + 任务栏按钮合并
- 「隐藏任务栏图标」和「恢复任务栏图标」合并成一个切换按钮
- 预览区新增点击反推坐标：点击后自动计算目标窗口中的实际坐标
- 点击命中客户区时，会把坐标自动回填到 Debug 的 `X / Y`
- 当预览显示完整窗口时，点击非客户区会明确提示，避免把标题栏坐标误用于脚本
- README 同步补充点图取坐标的使用方式

### v15 — 采样预览提速 + 移除预览保活
- 删除「预览保活」按钮及相关逻辑
- 采样预览的 BGRA→RGB 转换改为切片批量处理，去掉逐像素 Python 循环
- 采样预览刷新间隔从 700ms 降到 180ms
- 抓帧失败时改为 60ms 快速重试，而不是傻等下一轮
- `PrintWindow` 改为多 flags 轮询尝试，提升抓帧成功率

### v14 — 采样预览模式
- 主程序新增「开启采样预览 / 停止采样预览」按钮
- 采样预览会主动调用 `PrintWindow`，失败时退回到窗口位图抓取，不再完全依赖 DWM 缩略图
- 适用于“目标程序仍然活着，但 DWM 缩略图不再更新”的场景
- 取消锚定或关闭程序时会自动停止采样预览循环

### v12 — 任务栏图标隐藏
- 主程序新增「隐藏任务栏图标」和「恢复任务栏图标」按钮
- 通过切换 `WS_EX_APPWINDOW / WS_EX_TOOLWINDOW` 并刷新窗口框架，尝试隐藏任务栏按钮
- `恢复原位` 现在也会一并恢复任务栏图标相关的 `EXSTYLE`
- README 补充说明：DWM 预览拿到的是当前合成帧，不能强制游戏持续渲染

### v11 — 边缘保活模式
- 原“转后台运行”改为“转后台保活”，不再把窗口完全移出屏幕
- 改为把窗口停靠到屏幕边缘，只保留极小可见角，尽量避免游戏因为完全遮挡而停止渲染
- `script_action.py` 的后台运行逻辑同步改为边缘保活模式
- README 补充说明“完全离屏可能导致实时预览冻结”的原因

### v10 — 管理员重启
- 主程序标题栏新增权限状态显示
- 新增「管理员重启」按钮，可通过 UAC 直接重启为管理员权限
- 当日志出现 `err=5 / 拒绝访问` 时，会明确提示优先提权
- README 增加权限处理章节与新的推荐操作顺序

### v9 — Win32 错误码诊断
- `转后台运行` 和 `强制窗口化` 现在会记录 `ShowWindow / SetWindowPlacement / SetWindowPos / SetWindowLongW` 的错误码
- 主程序增加权限诊断：显示自身是否管理员、目标进程是否管理员
- README 补充说明如何通过 Win32 错误码判断是 `UIPI/权限问题` 还是 `窗口类型问题`

### v8 — 智能锚定真实窗口
- `锚定窗口` 不再直接使用下拉框中的标题句柄
- 会自动扫描同进程顶层窗口，并优先选择面积更大的真实渲染/输入窗口
- 日志面板新增同进程候选窗口列表，输出 `hwnd / area / class / title / rect`
- 解决“选中的是标题壳窗口，移动和点击都打不到真实游戏窗口”的问题

### v7 — 强制窗口化尝试
- 主程序新增「强制窗口化」按钮，尝试把目标进程的顶层窗口改成普通 `overlapped window`
- `转后台运行` 日志新增 `ShowWindow` / `SetWindowPlacement` / `SetWindowPos` 返回值
- `恢复原位` 现在也会恢复之前保存的窗口样式
- README 增加“先强制窗口化，再转后台运行”的推荐顺序

### v6 — 转后台运行增强诊断
- `转后台运行` 不再只移动当前句柄，而是会尝试移动同进程的所有顶层窗口
- 日志面板新增每个目标窗口的移动前后矩形，直接判断是否真的移出屏幕
- `恢复原位` 改为恢复同进程窗口组，而不是只恢复单个句柄
- README 补充“独占全屏/自管理窗口可能完全不接受标准 Win32 移动”的说明

### v5 — 后台运行模式 + 默认逻辑调整
- 主程序新增「转后台运行」和「恢复原位」按钮
- 不再建议对游戏使用最小化；默认改为“非最小化 + 屏幕外”后台运行模式
- `script_action.py` 默认先确保窗口保持可渲染状态，再执行后台消息点击
- 修复对子窗口点击时直接复用主窗口坐标的问题，改为先映射到子窗口客户区
- 把前台硬件点击改成可选回退，不再作为默认路径
- README 明确区分“DWM 预览存在”与“游戏仍在持续渲染/接受输入”并非同一件事

### v4 — 诊断 + 子窗口 + 策略5 + 日志面板
- 新增 `diagnose()` 诊断函数：检测管理员权限匹配、窗口类名、子窗口结构
- 新增策略5 `click_setcursor`：SetCursorPos + mouse_event，最接近真实鼠标操作
- 新增子窗口自动枚举，支持向子窗口逐个尝试发送点击
- GUI 新增诊断日志面板，脚本 log 输出实时显示在界面上
- 更新 README 加入完整变更日志

### v3 — 游戏多策略点击
- 重写 `script_action.py`，提供 4 种点击策略
- 新增 `window_foreground` 上下文管理器（临时前台化）
- 新增 SendInput 硬件级输入策略（策略3）
- 新增完整消息链策略（策略4）

### v2 — 最小化窗口支持
- 新增 `window_ready` 上下文管理器：屏幕外恢复最小化窗口
- 修复 Debug 放大镜坐标偏移：改用 `GetClientRect` 替代 `ScreenToClient`
- 新建窗口增加 100ms 延迟等待 Win32 窗口就位

### v1 — 基础功能
- 使用 DWM Thumbnail API 实现窗口实时预览（GPU 合成器直出）
- 支持最小化窗口画面渲染
- GUI：窗口选择、锚定、取消锚定、仅客户区切换
- 新增「执行脚本」按钮 + `script_action.py` 热重载
- 新增 Debug 放大镜：DWM rcSource 裁切 + 放大渲染
